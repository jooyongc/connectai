# 💻 코다리 — Connectai Risk Reduction Dashboard의 '구글맵/지역 리뷰 트렌드 변화율' API 연동 모듈을 최우선으로 재점검하고, 이 데이터가 Annual Risk Cost 로직에 오류 없이 실시간 반영되는지 검증하는 E-to-E 테스트 스크립트 3종(성공 케이스, 네트워크 에러 케이스, 데이터 포맷 오류 케이스)을 작성합니다. 시스템의 안정성을 최우선 목표로 합니다.

💻 코다리: 작업 시작합니다.

CEO 지시사항, 확실하게 이해했습니다. '구글맵/지역 리뷰 트렌드 변화율' 데이터 연동은 Connectai의 핵심적인 신뢰도 기반 리스크 점수 계산 로직에 직접 영향을 미치는 부분이므로, 안정성(Stability) 확보가 최우선입니다. ⚙️

단순히 코드만 짜서 끝내는 게 아닙니다. 이 API 호출과 데이터를 `Annual Risk Cost`로 변환하는 *전체 흐름*을 끊어서 테스트해야 합니다. 따라서 Mocking을 활용하여 성공/실패 케이스를 분리하고, 데이터 처리 로직(Data Pipeline) 자체에 초점을 맞춰 E-to-E 통합 테스트 스크립트를 작성하겠습니다.

현재 프로젝트 구조의 안정적인 설계를 위해 필요한 파일들을 먼저 생성 및 편집하겠습니다.

### 🏗️ 작업 계획
1.  **Mock API Client Layer 정의:** Google Maps API 호출을 시뮬레이션할 가짜 클라이언트 모듈을 만듭니다. (실제 API 키 노출 방지 및 테스트 용이성 확보)
2.  **Core Service 로직 통합:** 리스크 계산 서비스가 이 Mock API를 통해 데이터를 받고, 변환하는 핵심 함수를 정의합니다.
3.  **E-to-E 테스트 스크립트 작성:** 성공/네트워크 에러/포맷 에러 3가지 시나리오에 대한 Pytest 기반의 통합 테스트 케이스를 작성합니다.

---

### 🛠️ 코드 구현 (파일 생성)

먼저, 백엔드 서비스 로직과 테스트 모듈이 위치할 디렉토리 구조를 가정하고 파일을 만들겠습니다. 프로젝트 루트는 `/Users/project.adv/Connectai`라고 가정하겠습니다.

#### 1. Mock API Client 파일 생성 및 정의
API 호출 실패와 데이터 변형을 제어하기 위해 `mock_google_maps_client.py`를 만듭니다
